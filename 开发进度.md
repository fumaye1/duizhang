# 多云仓自动对账工具 - 开发进度文档

## 日期
- 2026-03-04
- 2026-03-05

## 本轮对话主要完成的功能

### 1) 主对账 MVP（Streamlit）
- 新增 `app.py`，完成主流程单页 UI（对照 HTML Demo 的主对账结构）。
- 支持 ERP 类型切换：`旺店通` / `云宝`。
- 支持上传并读取 Excel（`.xlsx/.xls`），按 Sheet 选择读取。
- 支持主流程参数配置：云仓、是否优先实际重量、是否启用扣减、是否启用耗材。

### 2) 对账核心计算逻辑（固定字段名）
- 新增 `reconcile.py`，实现：
  - 数据字段校验（`REQUIRED_COLUMNS` + `validate_columns`）。
  - 云宝货品名称映射为商家编码。
  - 省份清洗标准化。
  - 预估重量/实际重量取值逻辑（暂不含计泡体积）。
  - 结算重量按“重量段结束(kg)”向上匹配。
  - 资费匹配（云仓/快递公司/重量段/是否打包品/省份/生效期）。
  - 资费价格优先级：`快递费(元)` -> `快递费` -> `首重价格+续重价格`。
  - 撕单扣减、售后赔付扣减。
  - 云仓账单关联、差异金额与应付金额计算。
  - 输出三张结果：`对账结果` / `汇总` / `异常清单`。

### 3) 字段映射 UI
- 已在 `app.py` 中为主要数据源增加字段映射区：
  - 发货明细、云宝名称货品表、毛重表、重量段定义表、资费表、账单、耗材/撕单/售后。
- 支持：
  - 必填字段映射校验（未映射阻止执行）。
  - 映射列重复检测（同一源列被重复映射会提示并阻止执行）。

### 4) 字段映射模板保存/加载
- 在 UI 顶部新增“字段映射模板”区域。
- 支持 JSON 模板上传加载（自动回填到映射下拉框）。
- 支持当前映射导出为 JSON 模板下载。
- `README.md` 已补充模板使用说明与示例 JSON。

### 5) 文档与可运行性补充
- `README.md` 已更新运行命令、固定字段要求、输出说明、模板格式。
- 执行过语法校验：
  - `python -m py_compile app.py`
  - `python -m py_compile reconcile.py`

### 6) 多文件汇总导入（发货明细/云仓账单）
- 发货明细支持**多文件上传**并汇总：
  - 旺店通发货明细：多文件合并后再对账。
  - 云宝发货明细：多文件合并后再对账，并支持与旺店通明细**同一次对账合并**。
- 云仓账单支持**多文件上传**并汇总：
  - 适配“同一云仓按快递拆分多个账单文件”的场景。
  - 账单汇总口径：同一 `物流单号 + 云仓`，快递费/包装费求和，计费重量取最大。

### 7) 账单存在但发货明细缺失也输出
- 若发货明细不存在、但云仓账单存在：允许执行并在结果中输出账单行。
- 若某些单号仅存在于账单：仍会出现在“对账结果”里，并在“异常清单”标记为“发货明细缺失”。

### 8) 省份清洗逻辑增强（直辖市）
- 对“北京/上海/天津/重庆”直辖市，清洗后统一提取为不带“市”的名称：
  - 例如：`北京市 北京市 昌平区` -> `北京`；`北京 北京市 昌平区` -> `北京`。

---

## 未完成功能（按当前范围）

### A. 暂未实现/明确延期
- 计泡体积规则（用户已明确“暂不考虑”）。
- 全量业务模块（多仓汇总、分析、资费转换等）尚未在 Streamlit 中实现，目前以“单次对账”主流程为主。
- 字段映射模板的“本地持久化管理”（目前为上传/下载，不含模板列表、版本管理、历史记录）。
- 自动化测试（单元测试/集成测试）尚未建立，目前仅做语法级验证。

### B. 对账逻辑仍需强化
- 超重“续重递增”规则与不同计费口径的细节适配需进一步与业务规则逐条对齐。
- 异常清单分类粒度可继续细化（未匹配资费原因、日期越界、字段空值等）。
- 大数据性能优化（10万+）尚未专项优化与压测。
- 云仓账单与发货明细的关联目前按“物流单号”为主键；若同一物流单号跨云仓出现，需要明确是否需要增加“云仓”作为联合主键约束。

---

## 目前进度（截至 2026-03-05）

### 总体进度（主对账 MVP）
- 页面与流程：已打通（可上传 -> 映射 -> 计算 -> 预览 -> 下载）。
- 核心规则：已实现主干规则（不含计泡）。
- 配置与可用性：已具备字段映射模板保存/加载。
- 数据导入能力：发货明细/云仓账单均支持多文件汇总；账单存在但明细缺失也可输出结果。
- 测试状态：完成基础语法验证 + 小样例回归验证，待真实样例全链路验收。

### 交付文件
- `app.py`（Streamlit 入口与 UI）
- `reconcile.py`（主对账计算逻辑）
- `README.md`（运行与映射模板说明）
- `开发进度.md`（本文件）

---

## 调试过程记录（日期：2026-03-04）

### 1) 页面无法访问
- 现象：浏览器提示 `localhost 拒绝连接`。
- 排查：检查 Streamlit 启动终端输出，发现卡在 Streamlit 首次启动的 `Email:` 输入提示。
- 原因：进程等待交互输入，服务尚未真正监听端口。
- 处理：
  - 方案1：在该终端直接回车跳过邮箱输入。
  - 方案2：使用参数禁用统计提示重新启动：
    - `python -m streamlit run app.py --browser.gatherUsageStats false`
- 结果：服务可启动，页面可通过 `http://localhost:8501` 访问。

### 2) 功能增量调试
- 字段映射 UI 引入后，进行了语法检查并确认无语法错误。
- 模板加载容错：对 JSON 解析失败增加错误提示（格式错误/解析失败）。

---

## 下一步建议（建议顺序）
1. 用 `示例数据.md` 先构造一套真实 Excel，做端到端验收。
2. 补充“超重续重递增”细则与测试样例。
3. 增加自动化测试（至少 `reconcile.py` 的规则单元测试）。
4. 增加映射模板版本校验（`version`/`erp_type` 一致性提示）。
5. 若需要，可扩展到“多仓汇总/快递费分析”页面。
